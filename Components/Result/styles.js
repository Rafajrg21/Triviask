import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#0c064e',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });

export default styles;